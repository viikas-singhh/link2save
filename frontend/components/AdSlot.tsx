// Ads removed to keep link2save 100% free, fast, and clean.
export type AdPlacement = "top-banner" | "hero-bottom" | "result" | "between-sections" | "footer";

interface AdSlotProps {
  placement?: AdPlacement;
  className?: string;
}

export default function AdSlot(_props: AdSlotProps) {
  return null;
}
